# Submission Checklist

## Package checks

- [x] Explicit allowlist builder completed only at the canonical repository `submission/` path.
- [x] Realpath/component checks rejected paths outside the worktree and a disposable junction without touching its external sentinel.
- [x] An existing output requires the exact builder ownership marker; an unowned directory and sentinel were preserved.
- [x] `policies.sqlite` was rebuilt and `PRAGMA integrity_check` returned `ok`.
- [x] Portfolio `prototype/` full test suite passed: 257/257.
- [x] Packaged prototype test suite passed without a network or local model: 257/257.
- [x] Required five agent definitions, supervisor, projections, evidence/verifiers, and model-gateway contract are included.
- [x] Startup/operating inputs and declared/calculated comparisons are included.
- [x] Official policy source files and database are included.
- [x] Forbidden artifact/name and bounded content-signature scans are empty; quoted credential parameter-expansion branches are independently limited to explicit placeholders, and all packaged business/transaction/market demo records are explicitly synthetic.
- [x] `git diff --check` passed in the recorded release-gate run.

## Factual boundaries

- Prototype values are user-entered or synthetic; the Croatian restaurant is only a synthetic demo.
- Industry ranges are `PROTOTYPE_REFERENCE_RANGE`, not official averages.
- Policy records are versioned official-source snapshots; uncertain eligibility stays `CHECK_REQUIRED`.
- ChatGPT aided the prototype agent-result design; current runtime results are deterministic trusted templates.
- The production target is an enterprise internal local LLM behind the existing contract and verifiers.
- No local model was installed, run, or evaluated for this prototype.
- Market, nearby-store, and vacancy feeds remain `PLANNED_INTEGRATION`.
